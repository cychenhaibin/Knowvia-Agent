## 1. 服务端余额代理

- [x] 1.1 通过已加密的 FluxA 上游凭据获取状态配置和用户 `quota`，并校验必填数值与展示类型。
- [x] 1.2 发布受 Knowvia 会话保护的余额端点，使用 lower-camel 响应 DTO，并覆盖站点回退和安全错误映射。
- [x] 1.3 为上游解析、空值拒绝、认证错误和 HTTP 响应字段添加 Go 回归测试。

## 2. 移动端换算与数据访问

- [x] 2.1 定义 FluxA 余额 DTO，并实现仅携带 Knowvia Bearer Token 的余额 API 调用。
- [x] 2.2 实现 USD、CNY、CUSTOM、TOKENS 的纯余额格式化函数，以及无效输入降级。
- [x] 2.3 为四种展示类型、无效配置和 API 鉴权头添加 TypeScript 回归测试。

## 3. 资料页展示

- [x] 3.1 在资料页按用户与 FluxA 站点隔离余额查询，并在登出时清除其缓存。
- [x] 3.2 以格式化余额替换硬编码积分，并在用户分组标签后追加余额标签。
- [x] 3.3 覆盖资料页查询键、标签位置与失败隐藏行为，运行移动端及服务端完整验证。
